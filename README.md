# caio



## Como compilar o código

### FreeBSD

* Use clang com a seguinte linha de comando:
```Shell
clang++ binario.cpp -I/usr/local/include/opencv4 -L/usr/local/lib -lopencv_core -lopencv_highgui -lopencv_imgproc -lopencv_imgcodecs
```
